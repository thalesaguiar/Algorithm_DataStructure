#include <iostream>

using namespace std;

void selectionSort(int numbers[],int n){

    for(int i = 0; i < n; i++ ){
        int smallerindex = i;
    
        for(int j = i + 1; j < n; j++){
            if(numbers[j] < numbers[smallerindex])
            int a;
        }
    }

}